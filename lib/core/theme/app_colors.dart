import 'package:flutter/material.dart';

class AppColors {

  // Rosados elegantes tipo Pinterest / Notion
  static const Color primary = Color(0xFFD9A5B3); // dusty pink
  static const Color primaryDark = Color(0xFFC48A99); // rose muted
  static const Color primaryLight = Color(0xFFF5E9EC); // blush light

  // Acento suave
  static const Color accent = Color(0xFFE9D5DB);

  // Fondos minimalistas
  static const Color scaffoldBg = Color(0xFFFFFBFC); // blanco cálido
  static const Color cardBg = Color(0xFFFFFFFF);

  // Texto elegante
  static const Color textPrimary = Color(0xFF4F4A4B);
  static const Color textSecondary = Color(0xFF8A7E82);
  static const Color textOnPrimary = Colors.white;

  // Estados (más suaves, menos chillones)
  static const Color incomeGreen = Color(0xFF8FB996);
  static const Color expenseRed = Color(0xFFD98C8C);

  static const Color snackbarSuccess = Color(0xFF8FB996);

  // Extras para decoración
  static const Color borderSoft = Color(0xFFF0E4E8);
  static const Color shadowSoft = Color(0xFFF7EEF1);
}